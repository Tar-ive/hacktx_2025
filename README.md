# hacktx_2025

Enhanced UI prototype for the HackTX 2025 project. The `work` branch hosts a Three.js-powered
constellation view that sits on top of the untouched MVP backend. Use the commands below to run
the visualization locally:

```bash
npm install
npm run dev
```

The prototype focuses on visual polish and accessibility while keeping the underlying data flow
compatible with the MVP implementation.
